psycopg[binary]>=3.2,<4.0
requests>=2.31,<3.0
python-dateutil>=2.8,<3.0
tenacity>=8.2,<9.0

# Optional prod extras (uncomment if needed)
# uvloop>=0.19,<0.20 ; platform_system == "Linux"

# Dev/test (pin separately or in requirements-dev.txt)
# pytest>=7.4,<9.0
# pytest-cov>=4.1,<5.0
